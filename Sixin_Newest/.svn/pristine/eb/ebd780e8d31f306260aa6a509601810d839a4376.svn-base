package com.renren.mobile.chat.ui.groupchat;

import android.widget.Button;
import android.widget.EditText;
import android.widget.FrameLayout;
import android.widget.GridView;
import android.widget.HorizontalScrollView;
import android.widget.ImageView;
import android.widget.LinearLayout;
import android.widget.TextView;

import com.core.util.ViewMapping;
import com.renren.mobile.chat.R;
import com.renren.mobile.chat.ui.contact.C_ContactsListView;
import com.renren.mobile.chat.ui.contact.C_LetterBar;

/**
 * @author dingwei.chen1988@gmail.com
 * */
public class D_SelectContactsScreenHolder {
	
	@ViewMapping(ID=R.id.mGridView)
	public GridView mGridView;

    @ViewMapping(ID=R.id.select_contact_layout)
    public LinearLayout mSelectContactLayout = null;//联系人选择区域

	@ViewMapping(ID=R.id.contacts_listview)
	public C_ContactsListView mContactsListView = null;//联系人列表
	
	@ViewMapping(ID=R.id.pop_message)
	public TextView mPopText = null;//弹出字母
	
	@ViewMapping(ID=R.id.letter_bar)
	public C_LetterBar mLetterBar = null;//侧边索引条

    @ViewMapping(ID=R.id.search_layout)
    public FrameLayout mSearchLayout = null;//搜索栏
	
	@ViewMapping(ID=R.id.search_edit)
	public EditText mSearchEditText = null;//搜索框
	
	@ViewMapping(ID=R.id.search_del)
	public ImageView mImageDel = null;//清空文本按钮

    @ViewMapping(ID=R.id.search_divider)
    public ImageView mSearchDivider = null;//搜索栏下的分割线
	
	@ViewMapping(ID=R.id.no_search_data)
	public TextView mNoSearchData=null; //搜索没有结果时候显示

    @ViewMapping(ID=R.id.title_layout)
    public FrameLayout mLinearLayout;//Activity的title

    @ViewMapping(ID=R.id.horizontal_scroll_view)
    public HorizontalScrollView mHorizontalScrollView = null;//下部选中的联系人的横向滑动滚动条


    @ViewMapping(ID=R.id.select_group_done_button)
    public Button mSelectGroupDoneButton = null;//建群或者添加联系人的按钮

    @ViewMapping(ID=R.id.select_group_btn)
    public FrameLayout mSelectGroupBtn = null;//进入选择群页面的按钮

    @ViewMapping(ID=R.id.select_group_text)
    public TextView mSelectGroupText = null;//选择群安上上的文字

    @ViewMapping(ID=R.id.select_group_next)
    public ImageView mSelectGroupNext = null;//选择群按钮上的一个箭头图标
    
    @ViewMapping(ID=R.id.no_data_layout)
    public LinearLayout mNoDataLayout = null;//选择群按钮上的一个箭头图标
    
    @ViewMapping(ID=R.id.no_data_textView)
    public TextView mNoDataTextView = null;//选择群按钮上的一个箭头图标
    
}
