package com.common.emotion.view;
/**
 * 由EmotionViewFlipper,PointContainer或其子类负责实现，由能够实现切换的模块负责调用
 * 无论方向，只要是显示的内容切换了，就调用该接口。
 * 通常是调整进度用的。
 * @author zhenning.yang
 *
 */
public interface IOnFling2ContentChangeListener{
	public void onShowNextPointer();
	public void onShowPreviousPointer();
}