package com.sixin.widgets.layout;

import android.content.Context;
import android.os.Handler;
import android.os.Message;
import android.util.AttributeSet;
import android.widget.LinearLayout;

public class IncredibleListView extends LinearLayout {
    private static final int MSG_ANIMATION_ADDITION = 1000;

    public IncredibleListView(Context context) {
        this(context, null, 0);
    }

    public IncredibleListView(Context context, AttributeSet attrs) {
        this(context, attrs, 0);
    }

    public IncredibleListView(Context context, AttributeSet attrs, int defStyle) {
        super(context, attrs, defStyle);


    }

    private class AdditionAnimator {

        void compute() {

        }

        void animate() {

        }
    }

    private class AdditionHandler extends Handler {
        @Override
        public void handleMessage(Message msg) {
            switch (msg.what) {
                case MSG_ANIMATION_ADDITION:
                    break;
            }
        }
    }
}
